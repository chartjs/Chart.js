the edits that i have made do not interfere with the main flow rather it just checks the type of the input data, if it is a shorthand format then
the subroutine that i have defined adds some color to it and converts it into a usual data format.
